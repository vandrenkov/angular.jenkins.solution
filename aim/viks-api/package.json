{
  "name": "viks-api",
  "version": "0.0.0",
  "private": true,
  "description": "Demo API sibling to viks-webui for multi-component Jenkins pipelines",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test test/health.test.cjs",
    "build": "node scripts/build.cjs",
    // "test:ci": "ng test viks-webui --no-progress --watch=false --browsers=ChromeHeadlessCI --code-coverage",
    "test:headless": "ng test --no-watch --no-progress   --code-coverage --browsers=ChromeHeadless"
  },
  "engines": {
    "node": ">=18"
  }
}
